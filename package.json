{
  "name": "bodycam",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "npm run clean && node src 2>&1 | tee ./picam.log",
    "clean": "rm -rf hooks rec state && ./make_dirs.sh",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.740.0",
    "onoff": "^6.0.0",
    "tree-kill": "^1.2.2"
  }
}
